name: C/C++ CI

on:
  push:
    branches: [ 6 ]
  pull_request:
    branches: [ 6 ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: make
      run: make
